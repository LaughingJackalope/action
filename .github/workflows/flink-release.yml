name: Build and Release Apache Flink from Source

on:
  workflow_dispatch:
    inputs:
      flink_version:
        description: 'Apache Flink version to build'
        required: true
        type: choice
        options:
          - '2.0.0'
          - '2.1.0'
          - '2.2.0'
        default: '2.2.0'

# Minimal permissions for security and compliance
# - contents:write: Required to create GitHub Releases
# - packages:write: Required if publishing to GitHub Packages
permissions:
  contents: write
  packages: write

jobs:
  build-and-release:
    name: Build and Release Flink ${{ github.event.inputs.flink_version }}
    runs-on: ubuntu-latest
    
    steps:
      # ============================================================================
      # STEP 1: Check out Apache Flink source at the specified release tag
      # Purpose: Establish exact source provenance for regulatory audit trail
      # ============================================================================
      - name: Checkout Apache Flink source
        uses: actions/checkout@v6
        with:
          repository: apache/flink
          ref: release-${{ github.event.inputs.flink_version }}
          fetch-depth: 0
          fetch-tags: true

      # ============================================================================
      # STEP 2: Set up JDK 17 (Eclipse Temurin distribution)
      # Purpose: Use a vendor-supported, open-source JDK for reproducible builds
      # ============================================================================
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      # ============================================================================
      # STEP 3: Capture build environment details for audit trail
      # Purpose: Document exact build environment for compliance verification
      # ============================================================================
      - name: Capture build environment
        id: env_capture
        run: |
          echo "BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")" >> $GITHUB_ENV
          echo "JDK_VERSION=$(java -version 2>&1 | head -n 1)" >> $GITHUB_ENV
          echo "JDK_VENDOR=$(java -version 2>&1 | grep -i 'vendor' | head -n 1 || echo 'Eclipse Temurin (from setup-java)')" >> $GITHUB_ENV
          echo "RUNNER_OS=${{ runner.os }}" >> $GITHUB_ENV
          echo "RUNNER_ARCH=${{ runner.arch }}" >> $GITHUB_ENV
          echo "SOURCE_COMMIT=$(git rev-parse HEAD)" >> $GITHUB_ENV
          echo "SOURCE_COMMIT_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          echo "MAVEN_VERSION=$(mvn -version | head -n 1)" >> $GITHUB_ENV

      # ============================================================================
      # STEP 4: Build Flink from source
      # Purpose: Compile official Apache Flink release from verified source
      # ============================================================================
      - name: Build Flink
        run: |
          echo "Starting Flink build for version ${{ github.event.inputs.flink_version }}"
          echo "Source commit: ${{ env.SOURCE_COMMIT }}"
          mvn clean package -DskipTests -Pskip-webui-build 2>&1 | tee build-output.log
        timeout-minutes: 90

      # ============================================================================
      # STEP 5: Generate Software Bill of Materials (SBOM)
      # Purpose: Create compliance artifact for software supply chain transparency
      # Tool: Syft (open-source SBOM generator from Anchore)
      # ============================================================================
      - name: Install Syft
        uses: anchore/sbom-action/download-syft@v0

      - name: Generate SBOM
        run: |
          syft . --output spdx-json=flink-sbom-${{ github.event.inputs.flink_version }}.json
          syft . --output cyclonedx-json=flink-sbom-${{ github.event.inputs.flink_version }}-cyclonedx.json
          echo "SBOM files generated:"
          ls -la flink-sbom-*

      # ============================================================================
      # STEP 6: Create detailed build report for audit trail
      # Purpose: Provide human-readable documentation of build provenance
      # ============================================================================
      - name: Generate build report
        run: |
          cat > BUILD-REPORT-${{ github.event.inputs.flink_version }}.txt << 'EOF'
          ===========================================================================
          APACHE FLINK SOURCE BUILD - COMPLIANCE REPORT
          ===========================================================================
          
          BUILD IDENTIFICATION
          --------------------
          Source Repository:      apache/flink
          Release Tag/Version:  release-${{ github.event.inputs.flink_version }}
          Source Commit Hash:   ${{ env.SOURCE_COMMIT }}
          Build Timestamp:      ${{ env.BUILD_DATE }}
          GitHub Run ID:        ${{ github.run_id }}
          GitHub Run Number:    ${{ github.run_number }}
          
          BUILD ENVIRONMENT
          -----------------
          JDK Version:          ${{ env.JDK_VERSION }}
          JDK Vendor:           ${{ env.JDK_VENDOR }}
          Maven Version:        ${{ env.MAVEN_VERSION }}
          Runner OS:            ${{ env.RUNNER_OS }} ${{ env.RUNNER_ARCH }}
          
          SOURCE PROVENANCE
          -----------------
          This build was compiled from the official Apache Flink source code
          obtained directly from the apache/flink GitHub repository.
          
          The exact source commit used: ${{ env.SOURCE_COMMIT }} (short: ${{ env.SOURCE_COMMIT_SHORT }})
          This commit corresponds to the Git tag: release-${{ github.event.inputs.flink_version }}
          
          VERIFICATION INSTRUCTIONS
          -------------------------
          To verify the source provenance of this build:
          1. Clone the Apache Flink repository: git clone https://github.com/apache/flink.git
          2. Checkout the exact commit: git checkout ${{ env.SOURCE_COMMIT }}
          3. Verify the tag: git tag -v release-${{ github.event.inputs.flink_version }} (if signed)
          4. Compare build outputs with the artifacts in this release
          
          COMPLIANCE STATEMENT
          --------------------
          This build was produced using automated CI/CD with complete audit logging.
          All build inputs, environment details, and outputs are documented herein.
          The SBOM (Software Bill of Materials) is attached for dependency analysis.
          
          Generated by GitHub Actions workflow: ${{ github.workflow }}
          Workflow SHA: ${{ github.sha }}
          
          ===========================================================================
          END OF BUILD REPORT
          ===========================================================================
          EOF
          
          echo "Build report generated:"
          cat BUILD-REPORT-${{ github.event.inputs.flink_version }}.txt

      # ============================================================================
      # STEP 7: Upload build artifacts for release
      # Purpose: Preserve build outputs and compliance documentation
      # ============================================================================
      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: flink-build-${{ github.event.inputs.flink_version }}
          path: |
            BUILD-REPORT-${{ github.event.inputs.flink_version }}.txt
            flink-sbom-${{ github.event.inputs.flink_version }}.json
            flink-sbom-${{ github.event.inputs.flink_version }}-cyclonedx.json
            build-output.log
            flink-dist/target/*.jar
            flink-dist/target/*.tgz
            flink-dist/target/*.zip
            flink-runtime/target/*.jar
            flink-core/target/*.jar
            flink-streaming-java/target/*.jar
          if-no-files-found: warn
          retention-days: 90

      # ============================================================================
      # STEP 8: Create GitHub Release with all artifacts
      # Purpose: Establish immutable source of truth for built artifacts
      # ============================================================================
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: flink-${{ github.event.inputs.flink_version }}
          name: Apache Flink ${{ github.event.inputs.flink_version }} (Source Build)
          body: |
            ## Apache Flink ${{ github.event.inputs.flink_version }} - Source Build
            
            This release contains Flink ${{ github.event.inputs.flink_version }} built from the official Apache source.
            
            ### Source Provenance
            - **Source Repository:** https://github.com/apache/flink
            - **Source Tag:** `release-${{ github.event.inputs.flink_version }}`
            - **Source Commit:** `${{ env.SOURCE_COMMIT }}`
            
            ### Build Details
            - **Build Date:** ${{ env.BUILD_DATE }}
            - **JDK:** ${{ env.JDK_VERSION }}
            - **JDK Vendor:** ${{ env.JDK_VENDOR }}
            - **Maven:** ${{ env.MAVEN_VERSION }}
            - **Runner OS:** ${{ env.RUNNER_OS }} ${{ env.RUNNER_ARCH }}
            
            ### Compliance & Audit
            - Build report and SBOM are included for regulatory inspection
            - Full audit trail available in GitHub Actions logs
            - Source can be verified against Apache Flink official repository
            
            ### Files Included
            - `BUILD-REPORT-*.txt` - Detailed build and provenance information
            - `flink-sbom-*.json` - Software Bill of Materials (SPDX & CycloneDX formats)
            - Compiled Flink binaries and distributions
            
            ---
            *This release was generated automatically via GitHub Actions for compliance and traceability.*
          files: |
            BUILD-REPORT-${{ github.event.inputs.flink_version }}.txt
            flink-sbom-${{ github.event.inputs.flink_version }}.json
            flink-sbom-${{ github.event.inputs.flink_version }}-cyclonedx.json
            build-output.log
            flink-dist/target/*.jar
            flink-dist/target/*.tgz
            flink-dist/target/*.zip
            flink-runtime/target/*.jar
            flink-core/target/*.jar
            flink-streaming-java/target/*.jar
          fail_on_unmatched_files: false

      # ============================================================================
      # STEP 9: Output summary for workflow visibility
      # Purpose: Provide immediate feedback in GitHub Actions UI
      # ============================================================================
      - name: Output build summary
        run: |
          echo "::group::Build Summary"
          echo "✅ Flink ${{ github.event.inputs.flink_version }} build complete"
          echo "📦 Source Commit: ${{ env.SOURCE_COMMIT }}"
          echo "🏷️  Release Tag: flink-${{ github.event.inputs.flink_version }}"
          echo "📄 Build Report: BUILD-REPORT-${{ github.event.inputs.flink_version }}.txt"
          echo "🔍 SBOM Files: flink-sbom-${{ github.event.inputs.flink_version }}.json"
          echo "::endgroup::"
